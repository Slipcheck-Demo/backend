services:
  backend:
    build:
      context: .
    image: betway-booking-backend:prod
    restart: unless-stopped
    environment:
      PORT: 3000
      CORS_ORIGIN: ${CORS_ORIGIN}
    ports:
      - "8080:3000"
